# Python
__pycache__/
*.pyc

# uv / venv
.venv/

# 開発ツール
.playwright-mcp/

# Kaggle 認証情報（絶対にコミットしない）
kaggle.json
.kaggle/

# 巨大な公式生データ（再取得可能。Gitには載せない）
# 取得方法: uv run kaggle competitions download -c pokemon-tcg-ai-battle
data/reference/*.pdf
data/kaggle_raw/
*.zip

# ユーザー作成デッキ（ローカル保存用。空ディレクトリだけ管理）
data/user_decks/*.csv
data/user_decks/*.json
!data/user_decks/.gitkeep

# 登録AIエージェント（ローカル保存・実行コード）
data/user_agents/

# 対戦ログ
data/logs/

# 第三者のKaggle notebook（再取得可能・他者の著作物。リポジトリには含めない）
data/reference/kaggle_notebooks/

# 自作の提出物・実験用エージェント（ローカル作業用。git管理しない）
submissions/

# Kaggle環境シミュレータ(git clone。upstreamの別リポジトリなので含めない)
kaggle-environments/

# ローカル個人設定・作業メモ
.claude/settings.local.json
prompt.txt

# カード画像（著作権・大容量。履歴から除外。ローカル/ビルドには残す）
data/card_images/

.DS_Store

# 本番Basic認証の資格情報(VPSのみ・git管理外)
.env.caddy
.env

# AWSアカウント情報を含むためコミットしない
docs/ec2_burst_safety.md
