[project]
name = "pokeca-ai"
version = "0.1.0"
description = "社内有志 ポケカAIコンペ (PTCG-ABC) の開発・検証環境"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "kaggle>=1.6",
    "pymupdf>=1.27.2.3",
]

# このリポジトリ自体はライブラリとして配布しないため、パッケージ化しない
[tool.uv]
package = false
